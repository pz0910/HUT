# HUT - 主页美化方案 (SPEC v2)

## 1. 设计目标

| 项目 | 内容 |
|------|------|
| 变更范围 | 仅修改 index.html（CSS + JS 部分） |
| 不动 | manifest.json 结构、卡片跳转、搜索、分类筛选 |
| 核心目标 | 从"纯文字列表"升级为"有视觉冲击力的展示页面" |
| 技术约束 | 纯 CSS + JS，无外部依赖，无图片资源文件 |

## 2. 视觉设计方案

### 2.1 Hero 区域（新增）

```
┌─────────────────────────────────────────────┐
│  ✦ 渐变动画背景（CSS mesh gradient）         │
│                                             │
│           📦 HUT                            │
│     HTML 托管展示平台                        │
│     个人 HTML 作品集中管理                    │
│                                             │
│     [搜索框]                                │
│     [全部] [工具] [可视化] [演示] [实验] [论文] [其他] │
└─────────────────────────────────────────────┘
```

- **背景：** CSS mesh gradient 动画（4色渐变缓慢移动，8s 循环）
- **渐变色：** `#1f6feb` → `#238636` → `#8b5cf6` → `#1f6feb`（蓝绿紫循环）
- **叠加：** 半透明暗色遮罩 `rgba(13,17,23,0.85)` 保证文字可读
- **标题：** 增加微光效果（text-shadow 脉冲动画）
- **副标题：** 淡入上移动画（0.5s）

### 2.2 卡片缩略图（新增）

每张卡片顶部新增一个 **自动生成的封面区域**，不依赖外部图片：

```
┌──────────────────────────┐
│ ┌──────────────────────┐ │
│ │   🔬 论文             │ │  ← 分类图标 + 渐变背景
│ │   ═══════════════    │ │  ← 装饰性几何线条
│ └──────────────────────┘ │
│ 标题                     │
│ 描述文字...              │
│ 论文  ·  3天前           │
└──────────────────────────┘
```

**封面生成规则（纯 CSS）：**

| 分类 | 渐变色 | 图标 | 装饰图案 |
|------|--------|------|----------|
| 工具 | `#1f6feb` → `#388bfd` | 🔧 | 网格点阵 |
| 可视化 | `#238636` → `#3fb950` | 📊 | 波浪线 |
| 演示 | `#d29922` → `#e3b341` | 🎬 | 聚光灯圆 |
| 实验 | `#8b5cf6` → `#a78bfa` | 🧪 | 分子结构点 |
| 论文 | `#da3633` → `#f87171` | 📄 | 文档堆叠线 |
| 其他 | `#6e7681` → `#8b949e` | 📁 | 方块阵列 |

- 封面高度：140px
- 图标：居中，48px
- 装饰图案：CSS pseudo-element + background pattern
- 叠加暗色渐变底部 fade `transparent → rgba(0,0,0,0.6)`

### 2.3 动画体系

#### 2.3.1 卡片入场动画
- **效果：** 从下方淡入上移（translateY(20px) → 0, opacity 0 → 1）
- **交错：** 每张卡片延迟 50ms（第1张 0ms，第2张 50ms，第3张 100ms...）
- **时长：** 0.4s ease-out
- **触发：** 页面首次加载 + 筛选/搜索结果变化时

#### 2.3.2 卡片 hover 增强
- **现有：** translateY(-2px) + shadow（保留）
- **新增：** 封面区域微放大（scale 1.05，0.3s）
- **新增：** 卡片边框渐变色闪烁（border-color 过渡到分类主色）
- **新增：** 标题文字颜色变为分类主色

#### 2.3.3 Hero 区域动画
- **背景：** mesh gradient 8s infinite alternate
- **标题：** text-shadow 脉冲（2s infinite）
- **搜索框聚焦：** 发光效果（box-shadow 扩散分类色）

#### 2.3.4 标签切换动画
- **切换时：** 卡片列表淡出 → 重新排序 → 交错淡入
- **标签按钮：** hover 时微弹（scale 1.05）

### 2.4 统计信息栏（新增）

搜索框下方、标签栏上方，增加一行统计：

```
共 12 个作品  ·  6 个分类  ·  最近更新 3 天前
```

- 字号 12px，颜色 `var(--text-secondary)`
- 实时根据 filtered 结果更新数字
- 纯装饰性，增强页面信息密度

### 2.5 空状态增强

| 场景 | 当前 | 增强后 |
|------|------|--------|
| 无数据 | 📂 文字提示 | 📂 + 呼吸动画 + 渐变背景 + CLI 命令示例 |
| 无搜索结果 | 🔍 文字提示 | 🔍 + 搜索图标动画 + "试试其他关键词" |

## 3. 技术实现

### 3.1 CSS 变量扩展

```css
:root {
  /* 现有变量保持不变 */
  --bg: #0d1117;
  --card-bg: #161b22;
  /* ... */

  /* 新增分类主色（用于动画/缩略图） */
  --cat-tools: #58a6ff;
  --cat-viz: #3fb950;
  --cat-demo: #d29922;
  --cat-experiment: #a78bfa;
  --cat-paper: #f87171;
  --cat-other: #8b949e;

  /* 新增动画变量 */
  --anim-duration: 0.4s;
  --anim-stagger: 50ms;
}
```

### 3.2 新增 CSS 组件

| 组件 | 说明 |
|------|------|
| `.hero` | Hero 区域容器，mesh gradient 背景 |
| `.hero-bg` | 渐变动画层 |
| `.card-thumb` | 缩略图容器，140px 高 |
| `.card-thumb-icon` | 分类图标，48px 居中 |
| `.card-thumb-pattern` | 装饰图案层（pseudo-element） |
| `.stats-bar` | 统计信息栏 |
| `.fade-in-up` | 入场动画 class |
| `.stagger-N` | 交错延迟 class（N=0-20） |

### 3.3 JS 变更

| 变更点 | 说明 |
|--------|------|
| render() | 卡片 HTML 增加缩略图区域 |
| render() | 为每张卡片添加 fade-in-up class + stagger 延迟 |
| render() | 统计栏数字更新 |
| 分类切换 | 触发重新入场动画 |
| CSS_ANIM_MAP | 新增分类 → 颜色/图标映射表 |

### 3.4 性能预算

| 指标 | 目标 |
|------|------|
| 新增 CSS 体积 | < 3KB（gzip 后） |
| 新增 JS 体积 | < 1KB |
| 动画帧率 | 60fps（仅 transform/opacity 动画） |
| 首屏渲染 | < 2s（不变） |

## 4. 不变内容清单

以下功能和逻辑完全不动：

| 不动项 | 原因 |
|--------|------|
| manifest.json 数据结构 | 用户明确要求不动 |
| fetch 加载逻辑 | 用户明确要求不动 |
| 卡片跳转 `apps/{filename}` | 用户明确要求不动 |
| 分类标签筛选逻辑 | 用户明确要求不动 |
| 搜索过滤逻辑 | 用户明确要求不动 |
| 相对时间函数 | 功能正常 |
| escapeHtml 函数 | 功能正常 |
| 响应式断点 | 保留并增强 |

## 5. 页面布局变化

```
变化前:
┌─────────────────────────┐
│  📦 HUT                 │
│  副标题                  │
│  [搜索框]               │
│  [标签栏]               │
│  ┌────┐ ┌────┐ ┌────┐  │
│  │文字│ │文字│ │文字│  │
│  │卡片│ │卡片│ │卡片│  │
│  └────┘ └────┘ └────┘  │
└─────────────────────────┘

变化后:
┌─────────────────────────────┐
│  ✦✦✦ 渐变动画背景 ✦✦✦       │
│       📦 HUT (微光效果)      │
│       副标题 (淡入动画)       │
│       [搜索框 (聚焦发光)]     │
│       [统计栏: 共12个作品]    │
│       [标签栏 (hover弹跳)]   │
└─────────────────────────────┘
┌──────┐ ┌──────┐ ┌──────┐
│ 🔬   │ │ 📊   │ │ 🧪   │  ← 缩略图
│ 论文  │ │ 可视化│ │ 实验  │
│──────│ │──────│ │──────│
│ 标题  │ │ 标题  │ │ 标题  │  ← 交错入场
│ 描述  │ │ 描述  │ │ 描述  │
│ 标签  │ │ 标签  │ │ 标签  │
└──────┘ └──────┘ └──────┘
```

## 6. 验收标准摘要

| # | 验收项 |
|---|--------|
| 1 | Hero 区域 mesh gradient 动画正常运行 |
| 2 | 卡片顶部显示分类对应的缩略图（图标+渐变+装饰） |
| 3 | 卡片入场有交错淡入上移动画 |
| 4 | hover 时封面微放大 + 边框变色 + 标题变色 |
| 5 | 统计栏数字与实际数据一致 |
| 6 | 搜索/筛选切换有重新入场动画 |
| 7 | 所有原有功能（搜索/筛选/跳转）不受影响 |
| 8 | 暗色主题一致，无刺眼白色区域 |
| 9 | 移动端布局正常 |
| 10 | 无外部资源依赖（图片/CDN） |

## 封面图规范

- manifest.json 中 `"image"` 字段为可选，使用 Unsplash 直链格式
- **选图规则：每篇文章封面图必须包含美女元素**，搜索关键词：`woman + 文章主题`
- Unsplash 直链格式：`https://images.unsplash.com/photo-XXXXX?w=800&h=450&fit=crop`
- 有 image 字段时显示图片，无 image 字段时 fallback 到 CSS 默认图案+emoji 图标
